#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
  int cnt++;
  for (int i1 = 0; i1 < 3; i1++)
  {
    int marked[3];
    memset(marked, 0, sizeof(marked));
    for (int i2 = 0; i2 < 3; i2++)
    {
      
    }
  }
}
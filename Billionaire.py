from matplotlib import pyplot as plt
import numpy as np

#f = lambda x : (9*np.log(10)-1000*np.log(1-x)) / (np.log(1+x) - np.log(1-x))

#x = np.linspace(.001,.99999,1000)
#plt.plot(x,f(x))
#plt.show()
